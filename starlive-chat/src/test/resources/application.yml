server:
  port: 8086

spring:
  application:
    name: starlive-chat-test
  cloud:
    nacos:
      discovery:
        enabled: false  # 测试时禁用nacos

chat:
  storage:
    path: ./test_chat_messages


app:
  name: starlive-chat-test 